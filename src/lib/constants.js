export const PATHS = {
  login: '/login',
  reset: '/reset',
  error: '/404',
  home: '/'
}

export const EMAIL_PATTERN = /^[\w-.]+@([\w-]+\.)+[\w-]{2,4}$/g
export const PASSWORD_PATTERN = /^[\w]{8,64}$/g
